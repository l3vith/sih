# SIH - NWIS

Nearby Wells Intelligence System prototype for extracting DDR knowledge, mapping offset wells, and surfacing drilling-risk evidence.

## Run

```bash
npm install
npm run dev
```

For server-side Groq structuring, copy `.env.example` to `.env`, add a new `GROQ_API_KEY`, then run `npm run ocr-server` in another terminal.
